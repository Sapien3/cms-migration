## The filesystem is used as a cache for time consuming queries.

store.json field:

```
{
    "FeaturedMalafId": number,
    "SortedEditions": Array of { "id": string, date: string }
}
```
